// Warning: This file is only used in the development environment
// and is removed at build time.
// Do not edit the file unless necessary.

// The Vue.js DevTools Chrome extension (id: inncglfohabcebbkonjikolgejlglgpn)
// is incompatible with Electron's chrome-extension runtime — its MV3
// background script calls `chrome.action.setIcon`, which Electron's shim
// doesn't implement, throwing a TypeError into the renderer console on
// every reload. We use the standalone Vue DevTools (`@vue/devtools` v7,
// already a devDependency) instead. To inspect components in dev:
//
//   1. In a separate terminal: `npx @vue/devtools`
//   2. Add `<script src="http://localhost:8098"></script>` to index.html
//      OR `import '@vue/devtools'` once at the top of src/renderer/main.ts
//      while debugging (remove before committing).
//
// Auto-installing the broken extension is intentionally disabled here.
// import { installExtension, VUEJS_DEVTOOLS } from 'electron-extension-installer'
//
// installExtension(VUEJS_DEVTOOLS, {
//   loadExtensionOptions: {
//     allowFileAccess: true
//   }
// })
export {}
